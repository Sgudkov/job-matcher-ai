🔍 API: Поиск и фильтрация
📄 1. Поиск резюме

Endpoint:

``` sql
GET /resumes/search
```
Query параметры:

| Параметр         | Тип         | Описание                                                                                                |
| ---------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| `must_have`      | `list[str]` | Хард-скиллы, которые **обязательно** должны быть у кандидата. Пример: `?must_have=Python&must_have=SQL` |
| `should_have`    | `list[str]` | Хард-скиллы, которые **желательны** (повышают score, но не обязательны). Пример: `?should_have=Docker`  |
| `must_not_have`  | `list[str]` | Хард-скиллы, которых **не должно быть**. Пример: `?must_not_have=PHP`                                   |
| `soft_query`     | `str`       | Текстовое описание soft skills (векторный поиск). Пример: `?soft_query=team player with leadership`     |
| `grade`          | `str`       | Грейд: `junior`, `middle`, `senior`                                                                     |
| `experience_min` | `int`       | Минимальный опыт в годах                                                                                |
| `experience_max` | `int`       | Максимальный опыт в годах                                                                               |
| `salary_min`     | `int`       | Желаемая зарплата от                                                                                    |
| `salary_max`     | `int`       | Желаемая зарплата до                                                                                    |
| `location`       | `str`       | Город / страна / `remote`                                                                               |
| `limit`          | `int`       | Количество результатов (по умолчанию: 20)                                                               |
| `offset`         | `int`       | Смещение для пагинации                                                                                  |


📄 2. Поиск вакансий

Endpoint:

``` sql
GET /vacancies/search
```
Query параметры:

| Параметр         | Тип         | Описание                                                                               |
| ---------------- | ----------- | -------------------------------------------------------------------------------------- |
| `must_have`      | `list[str]` | Хард-скиллы, которые работодатель требует. Пример: `must_have=Go&must_have=SQL`        |
| `should_have`    | `list[str]` | Хард-скиллы, которые будут плюсом. Пример: `should_have=Kubernetes`                    |
| `must_not_have`  | `list[str]` | Навыки, которые работодатель не хочет видеть. Пример: `must_not_have=PHP`              |
| `soft_query`     | `str`       | Текстовое описание soft skills кандидата. Пример: `soft_query=responsible and proactive` |
| `grade`          | `str`       | Требуемый грейд: `junior`, `middle`, `senior`                                          |
| `experience_min` | `int`       | Минимальный опыт (лет)                                                                 |
| `experience_max` | `int`       | Максимальный опыт (лет)                                                                |
| `salary_min`     | `int`       | Вилка зарплаты от                                                                      |
| `salary_max`     | `int`       | Вилка зарплаты до                                                                      |
| `location`       | `str`       | Город / страна / `remote`                                                              |
| `limit`          | `int`       | Количество результатов (по умолчанию: 20)                                              |
| `offset`         | `int`       | Смещение для пагинации                                                                 |


📊 3. Пример запросов

``` sql
GET /resumes/search?must_have=Python&must_have=SQL&must_not_have=PHP&soft_query=team player
```
